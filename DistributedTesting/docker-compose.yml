version: "2"

services:
  master:
    image: satheeshpandianj/jmetermaster
    container_name: master
    tty: true
    hostname: master
    networks:
      - sats
  slave:
    image: satheeshpandianj/jmeterslave
    tty: true
    networks:
      - sats

networks:
  sats:
    driver: bridge
